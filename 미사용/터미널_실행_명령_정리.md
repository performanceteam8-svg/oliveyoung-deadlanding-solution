# 데드랜딩 점검 – 터미널 실행 명령 (처음부터)

아래를 **순서대로** 터미널에 입력하면 됩니다.

---

## 1. 프로젝트 폴더로 이동

```text
cd C:\Users\MADUP\vibecoding
```

*(실제 경로가 다르면 그 경로로 바꿔서 입력)*

---

## 2. 패키지 설치 (한 번만 하면 됨)

```text
pip install gspread google-auth
```

---

## 3. 연결 테스트 (선택)

```text
python spreadsheet_connect_test.py
```

- "연결 성공" 나오면 다음 단계로 진행

---

## 4. 데드랜딩 점검 실행

```text
python dead_landing_check_sheets.py
```

- 끝날 때까지 기다린 뒤, 구글 스프레드시트에서 **[URL]** 시트 새로고침(F5)

---

## 한 번에 복사해서 쓸 때 (이미 패키지 설치했다면)

```text
cd C:\Users\MADUP\vibecoding
python dead_landing_check_sheets.py
```

위 두 줄만 순서대로 실행하면 됩니다.
